import React, { PropTypes } from 'react'

function NotFound(props) {
    return (
        <section>
            <h3>Page not found</h3>
        </section>
    )
}

NotFound.propTypes = {

}

export default NotFound
